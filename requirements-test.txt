# Core ML dependencies for testing
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
pyyaml>=6.0
pydantic>=2.0
joblib>=1.3

# Testing tools
ruff>=0.4

# pyyaml above also serves ml-cicd's generate_ml_pipeline.py (workflow YAML
# self-validation) — the new ml-solution-design / ml-cicd / cost_estimator
# scripts are otherwise stdlib-only.

# Optional — only needed to run the live AWS validation scripts in
# tests/aws_validation/ (creates real, billable resources; see SECURITY.md),
# and by promote_model.py --registry sagemaker / mlflow at runtime:
# boto3>=1.34
# mlflow>=3.0

# Not installed in CI by design: apache-airflow and prefect. Their two
# pipeline scripts are platform definition files and are excluded via
# SKIP_HELP in tests/test_scripts.py.
